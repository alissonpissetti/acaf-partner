# URL da API NestJS (acaf-api).
# Em dev local, deixe vazio ou omita — o Vite faz proxy de /api para http://127.0.0.1:8787.
# Em build de produção, defina a URL pública da API (ex.: https://api.acafconnect.com.br).
VITE_API_URL=
